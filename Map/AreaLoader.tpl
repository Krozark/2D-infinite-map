#include <Config.hpp>

namespace map
{

    template<class T>
    void AreaLoader<T>::loadFromNetwork(Area<T>& area,int X,int Y)
    {
    };

    template<class T>
    void AreaLoader<T>::LoadFromFile(Area<T>& area,int X,int Y)
    {
    };
        

    
    
};
